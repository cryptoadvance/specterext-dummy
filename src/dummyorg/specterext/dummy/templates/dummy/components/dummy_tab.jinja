{% extends "<=ext.id =>/base.jinja" %}
{% block main %}
    <img src="{{ url_for(service.id +'_endpoint' + '.static', filename=service.logo) }}" width="300"/>    
    {% from '<=ext.id =>/components/<=ext.id =>_menu.jinja' import <=ext.id =>_menu with context %}
    {{ <=ext.id =>_menu(tab) }}
    {% block content %}
    {% endblock %}
{% endblock %}